class WorkoutModel{
  int ?id;
  String ? title;
  String ? timeLimit;
  String ? image;
  bool ? isSelected;
  String ? description;
  bool ? rep1;
  bool ? rep2;
  bool ? rep3;
 WorkoutModel({this.id, this.title, this.timeLimit, this.image, this.isSelected,this.description,this.rep1, this.rep2, this.rep3}); 

}